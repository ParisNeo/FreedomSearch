# Core dependencies
beautifulsoup4>=4.12.3
requests==2.32.3
setuptools==70.2.0
ascii_colors>=0.4.0
trafilatura>=1.12.0
tenacity>=9.0.0
cachetools>=5.3.0

# WARNING: googlesearch-python is unmaintained and has been associated
# with supply-chain incidents in the past. The Google engine is
# auto-disabled if this package is not installed (see google.py).
# For production deployments, replace it with an official API
# (SerpAPI, Google CSE, or Brave Search) and remove this line.
googlesearch-python==1.2.3
